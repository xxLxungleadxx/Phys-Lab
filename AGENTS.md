# Phys-Lab 開発ルール

このファイルは、このリポジトリで作業する Codex 向けの恒常ルールである。設計判断は `docs/DESIGN.md`、教材の公開基準は `docs/CONTENT_POLICY.md`、公開前検証項目は `Phys-Lab_Codex_Development_Guide.md` の第38章を参照する。ローカルの `docs/CHECKLIST.md` が存在する場合は、その詳細項目も使う。

## 1. サイトの目的

Phys-Lab は、高校物理を学ぶ人が「読む・見る・動かす」ことで理解を深めるための、個人運営の公開教材サイトである。特定の学校、授業、クラスだけを対象にしない。

実装では次を優先する。

1. 学習者が単元から教材を探せること
2. 教材の対象レベルと目的が分かること
3. PC、iPad、スマートフォンで利用できること
4. 既存の公開 URL と機能を壊さないこと
5. 静的サイトとして単純で保守しやすいこと

## 2. 技術方針

- 当面は GitHub Pages と Vanilla HTML / CSS / JavaScript を維持する。
- 図には SVG、動的な可視化には Canvas API を必要に応じて使う。
- 明確な必要性がないライブラリ、フレームワーク、外部 CDN、Web フォントを追加しない。
- CSS では既存の変数と共通クラスを優先し、ページ固有の重複を増やさない。
- JavaScript は教材固有の処理に限定し、重要情報を JavaScript または Canvas 内だけに置かない。
- 同じ共通レイアウト変更が多数のページに継続して必要になった場合に限り、静的サイトジェネレーターを別タスクとして検討する。

## 3. URL とナビゲーション

- 公開済み URL は原則維持する。移動が必要な場合は旧 URL から到達できる互換経路を残す。
- 新しい URL は英小文字とハイフンを使い、内容が分かる名前にする。
- トップページと主要ナビゲーションでは「単元から探す」を「教材の種類から探す」より優先する。
- 単元名と教材形式の正式な分類・パスは `docs/DESIGN.md` に従う。
- 相対リンクは GitHub Pages のプロジェクトサイト `/Phys-Lab/` 配下で動作することを確認する。

## 4. 教材ページの必須要素

新規または大幅改修する教材には、原則として次を表示する。

- タイトル
- 単元
- 対象レベル（物理基礎・物理・発展）
- 教材形式（読む・見る・動かす・外部リンク）
- 概要
- 教材本体
- 更新日
- 関連教材（該当する場合）

分類値とメタデータの記述方法は `docs/CONTENT_POLICY.md` に従う。色だけで対象レベルや状態を表さない。

## 5. アクセシビリティとレスポンシブ対応

- semantic HTML を使い、各ページの主題を `h1` とし、見出しを `h1` → `h2` → `h3` の順にする。
- 意味のある画像には内容を伝える `alt`、装飾画像には空の `alt` または適切な `aria-hidden` を設定する。
- 主要操作はキーボードで利用可能にし、見えるフォーカス表示を保つ。
- ホバーだけに依存せず、ボタンやリンクには十分なタップ領域を確保する。
- ページ全体の横スクロールを発生させない。Canvas、数式、表は狭い画面でも扱えるようにする。
- Canvas の結果、値、条件は HTML テキストでも確認できるようにする。
- 動きを追加する場合は `prefers-reduced-motion` を考慮する。
- デスクトップ、iPad 横・縦、スマートフォン横・縦を標準対象とし、特に iPad Safari を重視する。

## 6. シミュレーション教材

- パラメータ変更と物理量の関係を確認できる教材にする。
- 初期表示だけでも学習上意味のある状態にする。
- 数値、グラフ、軌跡、ベクトルを目的に応じて組み合わせる。
- 最小値、最大値、0、境界値などを確認し、例外的入力で表示や計算が破綻しないようにする。
- 単位を明示し、丸めは表示時に行う。
- 「空気抵抗を無視」「重力加速度は一定」「質点として扱う」など、モデルの前提を明示する。
- 物理内容と教材としての難易度は、公開前に人間のレビューを受ける。

## 7. 公開情報と安全性

次をリポジトリへ追加・コミットしない。

- 生徒氏名、クラス名、成績などの個人情報
- 学校名、学校メールアドレス、校内 URL、校内 IP
- Teams / SharePoint などの内部 URL
- API キー、トークン、パスワード、秘密鍵、`.env`
- ローカル管理用 Obsidian ノートや `.DS_Store`

外部の画像・本文・問題文を無断転載しない。外部教材は原則リンクで紹介する。PDF は公開前にファイル名と Author / Creator / Producer などのメタデータを確認する。

## 8. Git 運用

- `dev` を開発、`main` を公開ブランチとする。
- Codex は原則 `dev` で作業し、人間の確認前に `main` へ反映しない。
- ユーザーの既存変更や未追跡ファイルを、依頼なく削除・上書き・コミットしない。
- コミットまたは push は、ユーザーが明示的に依頼した場合に行う。
- 履歴を書き換える操作や強制 push は、明示的な依頼なしに行わない。

## 9. 標準作業フロー

各タスクでは、規模に応じて次を行う。

1. 現状と関連ファイルを調査する。
2. 要件、既存 URL、回帰リスクを整理する。
3. 実装方針を定める。
4. 既存設計に沿って最小限の変更を実装する。
5. 自動検証とローカルリンク確認を行う。
6. 対象画面幅で表示と操作を確認する。
7. `git diff` と `git status` で意図しない変更がないか確認する。
8. 必要な文書を更新する。
9. 未確認事項と人間による確認事項を報告する。

公開候補の変更では総合ガイド第38章と、存在する場合はローカルの `docs/CHECKLIST.md` を使う。検証に使える環境がない項目は、合格扱いにせず「未確認」として報告する。ブラウザ検証はCLIの `npm test` で実行し、組み込みブラウザへの依存を必須にしない。検証文書・監査記録・レポート・生成結果はGit対象外とし、検証コードと設定のみを追跡する。

## 10. 完了報告

完了時は、必要な項目に絞って次を報告する。

- 変更概要
- 変更ファイル
- 実装内容
- テスト内容と結果
- 確認済み環境
- 未確認事項
- 既知の問題または回帰リスク
- 今後の改善候補

Codex による自動検証は公開品質の最終保証ではない。物理内容、難易度、説明文、UI の直感性、公開可否は人間が最終判断する。
